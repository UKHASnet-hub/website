<?php
// Returns info (callsign, owner, description) on a node, or all nodes

if(isset($_REQUEST['node_id'])) {
    // Return info for one node
} else {
    // Return info for all nodes
}
print json_encode (json_decode ("{}"));
?>
