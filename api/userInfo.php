<?php
// Returns info (name, description, nodes status summary) on a user, or all users

if(isset($_REQUEST['user_id'])) {
    // Return info for one user
} else {
    // Return info for all users
}
print json_encode (json_decode ("{}"));
?>
